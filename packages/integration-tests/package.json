{
  "name": "@repo/integration-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test:generation-time-migration": "vitest run --config vitest.config.ts src/generation-time-migration.test.ts",
    "test:moderation": "vitest run --config vitest.config.ts src/moderation-policy-service.test.ts",
    "test:release-governance": "vitest run --config vitest.config.ts src/release-governance-gate.test.ts",
    "test:video-generation-recovery": "vitest run --config vitest.config.ts src/video-generation-recovery.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@repo/database": "workspace:*",
    "@repo/shared": "workspace:*",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@types/node": "20.19.43",
    "@types/pg": "^8.20.0",
    "typescript": "^5",
    "vitest": "4.1.9"
  },
  "license": "AGPL-3.0-only"
}
