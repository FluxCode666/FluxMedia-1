{
  "name": "gpt2image-pro",
  "version": "0.8.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/with-root-env.mjs scripts/dev.mjs",
    "prebuild": "pnpm cache:trim",
    "build": "turbo build",
    "pretest": "pnpm cache:trim",
    "test": "node --test scripts/turbo-cache.test.mjs && turbo test",
    "pretypecheck": "pnpm cache:trim",
    "typecheck": "turbo typecheck",
    "prelint": "pnpm cache:trim",
    "lint": "turbo lint",
    "cache:clean": "node scripts/turbo-cache.mjs clean",
    "cache:trim": "node scripts/turbo-cache.mjs trim",
    "dev:web": "node scripts/with-root-env.mjs ./node_modules/turbo/bin/turbo dev --env-mode=loose --filter=@repo/web",
    "prebuild:web": "pnpm cache:trim",
    "build:web": "turbo build --filter=@repo/web",
    "db:generate": "turbo db:generate --filter=@repo/database",
    "db:push": "turbo db:push --filter=@repo/database",
    "db:studio": "turbo db:studio --filter=@repo/database"
  },
  "devDependencies": {
    "dotenv": "^16.5.0",
    "turbo": "^2.9.18"
  },
  "packageManager": "pnpm@10.27.0",
  "pnpm": {
    "overrides": {
      "kysely": "0.28.17"
    }
  },
  "license": "AGPL-3.0-only"
}
