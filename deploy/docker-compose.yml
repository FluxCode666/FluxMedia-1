# FluxMedia 生产 Compose：运行 Next.js 主服务与 Adobe direct 专用代理。
# PostgreSQL、Redis 与其他可选上游均为外部依赖。
name: fluxmedia

services:
  # 数据库迁移是一次性维护任务，默认 profile 不启用，普通 up 只启动 web。
  migrate:
    profiles:
      - maintenance
    image: ${FLUXMEDIA_MIGRATE_IMAGE:-ghcr.io/fluxcode666/fluxmedia-migrate}:${FLUXMEDIA_TAG:?FLUXMEDIA_TAG 必填}
    restart: "no"
    env_file:
      - .env
    environment:
      DATABASE_URL: ${DATABASE_URL:?DATABASE_URL 必填}
      LOCAL_STORAGE_PATH: /app/storage
      NODE_ENV: production
      TZ: UTC
      VIDEO_INPUT_ROLLBACK_MANIFEST: /app/deploy-state/video-input-rollback-${FLUXMEDIA_TAG:?FLUXMEDIA_TAG 必填}.ndjson
    volumes:
      - app-storage:/app/storage
      - ./state:/app/deploy-state

  web:
    image: ${FLUXMEDIA_IMAGE:-ghcr.io/fluxcode666/fluxmedia-web}:${FLUXMEDIA_TAG:?FLUXMEDIA_TAG 必填}
    restart: unless-stopped
    init: true
    networks:
      - flux-code
      - infra
    env_file:
      - .env
    # 每个 web 容器只运行一个 Node 主进程；下列 Worker Thread 数按
    # Node 进程计算，水平扩容 web 副本时容量会随副本数线性增加。
    environment:
      DATABASE_URL: ${DATABASE_URL:?DATABASE_URL 必填}
      BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET:?BETTER_AUTH_SECRET 必填}
      BETTER_AUTH_URL: ${BETTER_AUTH_URL:-https://media.flux-code.cc}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL:-https://media.flux-code.cc}
      APP_TIME_ZONE: ${APP_TIME_ZONE:-Asia/Shanghai}
      LOCAL_STORAGE_PATH: /app/storage
      # pg 按进程 TZ 解析无时区 timestamp；展示时区只能通过 APP_TIME_ZONE 配置。
      TZ: UTC
      ADOBE_DIRECT_PROXY_URL: ${ADOBE_DIRECT_PROXY_URL:-http://media-upstream-proxy:3021}
      ADOBE_DIRECT_PROXY_SECRET: ${ADOBE_DIRECT_PROXY_SECRET:?ADOBE_DIRECT_PROXY_SECRET 必填}
      ADOBE_DIRECT_PROXY_TIMEOUT_SECONDS: ${ADOBE_DIRECT_PROXY_TIMEOUT_SECONDS:-300}
      ADOBE_DIRECT_PROXY_MAX_BODY_MB: ${ADOBE_DIRECT_PROXY_MAX_BODY_MB:-64}
      API_UPSTREAM_SCRIPT_WORKER_COUNT: ${API_UPSTREAM_SCRIPT_WORKER_COUNT:-1}
      API_UPSTREAM_SCRIPT_MEMORY_LIMIT_MB: ${API_UPSTREAM_SCRIPT_MEMORY_LIMIT_MB:-32}
      API_UPSTREAM_SCRIPT_STACK_LIMIT_KB: ${API_UPSTREAM_SCRIPT_STACK_LIMIT_KB:-512}
    ports:
      - "${BIND_HOST:-127.0.0.1}:${WEB_PORT:-3000}:3000"
    volumes:
      - app-storage:/app/storage
    depends_on:
      media-upstream-proxy:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:3000/').then((response) => process.exit(response.status < 500 ? 0 : 1)).catch(() => process.exit(1))",
        ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    security_opt:
      - no-new-privileges:true
    stop_grace_period: 30s
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
    logging:
      driver: json-file
      options:
        max-size: "100m"
        max-file: "5"

  media-upstream-proxy:
    image: ${FLUXMEDIA_PROXY_IMAGE:-ghcr.io/fluxcode666/fluxmedia-media-upstream-proxy}:${FLUXMEDIA_TAG:?FLUXMEDIA_TAG 必填}
    restart: unless-stopped
    init: true
    networks:
      - flux-code
    environment:
      ADOBE_DIRECT_PROXY_SECRET: ${ADOBE_DIRECT_PROXY_SECRET:?ADOBE_DIRECT_PROXY_SECRET 必填}
      ADOBE_DIRECT_PROXY_TIMEOUT_SECONDS: ${ADOBE_DIRECT_PROXY_TIMEOUT_SECONDS:-300}
      ADOBE_DIRECT_PROXY_MAX_BODY_MB: ${ADOBE_DIRECT_PROXY_MAX_BODY_MB:-64}
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "wget -qO- --header='X-Proxy-Secret: '$${ADOBE_DIRECT_PROXY_SECRET} http://127.0.0.1:3021/healthz >/dev/null || exit 1",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    security_opt:
      - no-new-privileges:true
    stop_grace_period: 30s
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "5"

volumes:
  app-storage:

networks:
  infra:
    external: true
    name: infra_default
  flux-code:
    external: true
    name: flux-code
