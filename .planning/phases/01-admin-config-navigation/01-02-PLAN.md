---
phase: 01-admin-config-navigation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - "apps/web/src/features/dashboard/sidebar-navigation.ts"
  - "apps/web/src/features/dashboard/sidebar-navigation.test.ts"
  - "apps/web/src/features/dashboard/components/sidebar.tsx"
  - "apps/web/messages/en.json"
  - "apps/web/messages/zh.json"
  - "apps/web/src/features/dashboard/navigation-i18n-contract.test.ts"
autonomous: true
requirements:
  - NAV-04
  - PRES-02
  - VER-01
  - VER-03
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "observer_admin、admin、super_admin 的 Administration 菜单显示模型配置和供应商管理；只有 super_admin 显示系统设置；普通 user 不获得管理入口。"
    - "模型配置和供应商管理在带 locale 的桌面展开、桌面折叠和移动 Sheet 状态下使用同一最长前缀 active 判定，点击移动菜单后关闭 Sheet，折叠态仍提供完整 tooltip/title。"
    - "中英文 nav/pages 消息都提供精确的模型配置与供应商管理文案，侧栏标题映射不再把供应商入口显示为账号池。"
    - "导航契约、角色/路由聚焦测试、Web typecheck 和 Web lint 能在同一阶段验证命名与激活回归。"
  artifacts:
    - "可复用的 sidebar role menu 与 locale 路径 active 判定纯函数及 DB-free 测试。"
    - "更新后的 DashboardSidebar 管理入口和展开/折叠/移动行为。"
    - "zh/en Dashboard.nav 与 Dashboard.pages 的 modelConfiguration、supplierManagement 文案及契约断言。"
  key_links:
    - "sidebar role menu 必须引用 `/dashboard/admin/model-configuration` 与 `/dashboard/admin/suppliers`，并通过 `localizedHref` 进入 Phase 1 页面。"
    - "active 判定必须在 sidebar 的父项/子项渲染、折叠状态和移动 Sheet 共用，最长路径优先以避免父级 admin 路由抢占激活。"
    - "消息键、title map 和页面标题使用同一 modelConfiguration/supplierManagement 命名，内部 image-backend-pool operation 名称不参与外显翻译。"
---

<objective>
把已完成的独立页面接入管理员侧栏，并锁定中英文文案和所有侧栏状态的路由激活契约。

Purpose: 按 D-01、D-02、D-05、D-07 让有权角色能从真实菜单发现页面，避免仅靠 URL 访问；把 locale、角色菜单、active 指示和移动端关闭行为收敛到可测试的导航契约。
Output: 角色化管理菜单、最长前缀 active helper、双语消息更新、导航契约测试和聚焦验证命令。
</objective>

<execution_context>
@/Users/duegin/.codex/gsd-core/workflows/execute-plan.md
@/Users/duegin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-admin-config-navigation/01-CONTEXT.md
@.planning/phases/01-admin-config-navigation/01-UI-SPEC.md
@.planning/phases/01-admin-config-navigation/01-01-PLAN.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@apps/web/src/features/dashboard/components/sidebar.tsx
@apps/web/src/features/dashboard/navigation-i18n-contract.test.ts
@apps/web/messages/zh.json
@apps/web/messages/en.json
@packages/shared/src/auth/roles.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 打通角色化侧栏到独立页面的 active 导航路径</name>
  <files>apps/web/src/features/dashboard/sidebar-navigation.ts, apps/web/src/features/dashboard/sidebar-navigation.test.ts, apps/web/src/features/dashboard/components/sidebar.tsx</files>
  <action>按 D-01、D-04、D-05、D-07 将现有 sidebar 中的管理员菜单构建和最长路径匹配抽到 DB-free 的 `sidebar-navigation.ts`：提供按 `AppUserRole` 生成管理项的纯函数（observer_admin 仅模型配置/供应商管理，admin 同样不显示高敏系统设置，super_admin 额外显示系统设置）以及去 locale 前缀、选择最长匹配 href 的 active 函数。`DashboardSidebar` 只消费这些函数，保留现有 Administration/User 分组、父子支付菜单、折叠后 tooltip/title、点击折叠项先展开、移动 Sheet 点击后关闭和 active 背景/左侧指示线；新增项的 href 精确指向 `/dashboard/admin/model-configuration`、`/dashboard/admin/suppliers` 和 `/dashboard/admin/settings`，并让 title map 使用新消息键。不要把菜单可见性当成授权边界，也不要修改页面守卫、Action/UOL 或用户功能菜单。测试以表驱动覆盖四角色菜单 href/title、locale 前缀和深层路径最长匹配，证明同一 active 结果可供展开/折叠/移动渲染复用。 </action>
  <verify>
    <automated>pnpm --filter @repo/web exec vitest run "src/features/dashboard/sidebar-navigation.test.ts"</automated>
    <automated>pnpm --filter @repo/web typecheck</automated>
  </verify>
  <done>三档后台角色从侧栏可到达两个独立页面，只有 super_admin 看到系统设置；locale 前缀和深层路径的 active href 由同一最长匹配 helper 计算，桌面折叠与移动 Sheet 的既有交互没有回归。</done>
</task>

<task type="auto">
  <name>Task 2: 锁定中英文导航文案与阶段聚焦验证</name>
  <files>apps/web/messages/en.json, apps/web/messages/zh.json, apps/web/src/features/dashboard/navigation-i18n-contract.test.ts</files>
  <action>按 D-02、D-05、D-07 在 `Dashboard.nav` 和 `Dashboard.pages` 中同步增加 `modelConfiguration` 与 `supplierManagement`：中文精确为“模型配置”“供应商管理”，英文精确为“Model Configuration”“Supplier Management”；同步加入模型/供应商页面使用的只读提示和主 CTA 文案键，保证面板根标题与侧栏显示不依赖硬编码 locale。替换 sidebar 使用的旧账号池外显键并清理无引用的旧 nav/pages 展示键，但保留 packages/shared、UOL、数据库、调度及 feature 内部的 image-backend-pool 标识。扩展 `navigation-i18n-contract.test.ts`，用精确断言锁定两种 locale 的 nav/pages 文案、旧账号池菜单不可作为新入口、角色化菜单中的三个 href，并保留既有生成/看板契约。完成阶段聚焦测试、Web typecheck 和 Web lint；不引入新的 UI 或 i18n 依赖。 </action>
  <verify>
    <automated>pnpm --filter @repo/web exec vitest run "src/features/dashboard/navigation-i18n-contract.test.ts" "src/features/dashboard/sidebar-navigation.test.ts" "src/app/[locale]/(dashboard)/dashboard/admin/model-configuration/page.test.ts" "src/app/[locale]/(dashboard)/dashboard/admin/suppliers/page.test.ts" "src/app/[locale]/(dashboard)/dashboard/admin/settings/page.test.ts"</automated>
    <automated>pnpm --filter @repo/web typecheck</automated>
    <automated>pnpm --filter @repo/web lint</automated>
  </verify>
  <done>zh/en 消息和导航契约都锁定两个独立入口的精确文案；角色、路由守卫、页签装配和 active 状态聚焦测试通过，Web typecheck/lint 全绿，旧内部领域名仍只在内部语义中使用。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 会话角色 → 侧栏菜单 | 客户端会话可能为空或带未知 role，菜单计算不能把展示误当授权。 |
| locale/pathname → active helper | URL 路径是不可信字符串，必须规范化并避免父路由误匹配。 |
| 消息 JSON → 页面渲染 | 双语消息是构建时输入，键缺失或外显内部名会造成信息误导。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Elevation of Privilege | sidebar role menu | medium | mitigate | 角色化菜单只影响发现性；独立页面继续执行 Phase 1 服务端守卫，测试覆盖 user/observer/admin/super_admin 组合，防止以隐藏或显示状态替代权限。 |
| T-01-06 | Tampering | active pathname matching | medium | mitigate | 纯 helper 去 locale 前缀并按最长 href 匹配，父级和深层管理路径在展开、折叠、移动渲染中共享结果。 |
| T-01-07 | Information Disclosure | Dashboard.nav/pages messages | low | mitigate | 精确契约断言阻止把 image-backend-pool 内部 operation/领域名作为用户可见菜单标题，并确保中英文键同步。 |
</threat_model>

<verification>
<automated>pnpm --filter @repo/web exec vitest run "src/features/dashboard/sidebar-navigation.test.ts" "src/features/dashboard/navigation-i18n-contract.test.ts" "src/app/[locale]/(dashboard)/dashboard/admin/model-configuration/page.test.ts" "src/app/[locale]/(dashboard)/dashboard/admin/suppliers/page.test.ts" "src/app/[locale]/(dashboard)/dashboard/admin/settings/page.test.ts"</automated>
<automated>pnpm --filter @repo/web typecheck</automated>
<automated>pnpm --filter @repo/web lint</automated>
<human-check>启动 Web 后分别以 observer_admin、admin、super_admin 检查中英文侧栏：展开态、桌面折叠态和移动 Sheet 均能看到对应 active 背景/左侧指示；移动端点击模型配置或供应商管理后 Sheet 关闭；observer_admin 不显示系统设置入口。</human-check>
</verification>

<success_criteria>
- Administration 菜单按角色提供模型配置、供应商管理和必要的系统设置入口，所有 href 带 locale 后仍能正确导航。
- 展开、折叠、移动三种侧栏状态使用统一最长前缀 active 逻辑，父级支付菜单和用户菜单不回归。
- zh/en nav/pages 精确提供 Model Configuration / 模型配置、Supplier Management / 供应商管理，并通过契约测试锁定。
- 阶段聚焦测试、Web typecheck、Web lint 通过，且未引入数据库、调度、账务或 UOL 领域改动。
</success_criteria>

<output>
Create `.planning/phases/01-admin-config-navigation/01-02-SUMMARY.md` when done
</output>
